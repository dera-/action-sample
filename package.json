{
  "name": "@dera-/action-sample",
  "private": true,
  "version": "1.0.8",
  "description": "",
  "scripts": {
    "prepare": "npm run build",
    "build": "npm run build:release",
    "build:release": "ncc build release/src/index.js -o ./release/dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/github": "^6.0.1",
    "@jsdevtools/npm-publish": "^1.4.3",
    "npm-run-all": "^4.1.5",
    "simple-git": "^3.30.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
